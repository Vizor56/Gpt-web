services:
  - type: web
    name: yaroslav-tutor-school
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: AUTO_MIGRATE
        value: true
      - key: DATABASE_URL
        sync: false
